version: 2.1

setup: true

jobs:
  choose-config:
    docker:
      - image: cimg/base:stable
    steps:
      - run: circleci config process .circleci/angular.yml > /tmp/processed.yml
workflows:
  setup:
    jobs:
      - choose-config
